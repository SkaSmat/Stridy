# ==============================================================================
# CITY EXPLORER - ENVIRONMENT VARIABLES
# ==============================================================================
# IMPORTANT: Copy this file to .env and fill in your actual values
# The .env file should NEVER be committed to Git (.gitignore is configured)
# ==============================================================================

# ------------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Unified Instance with PostGIS)
# ------------------------------------------------------------------------------
# We use a single Supabase instance for everything (auth + geo data)
# Instance ID: anujltoavoafclklucdx (your external PostGIS-enabled instance)

VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key-here"

# How to get these values:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project (anujltoavoafclklucdx or your instance)
# 3. Settings → API
# 4. Copy:
#    - Project URL → VITE_SUPABASE_URL
#    - anon/public key → VITE_SUPABASE_PUBLISHABLE_KEY
# 5. Project ID is visible in the URL

# ------------------------------------------------------------------------------
# STRAVA API CONFIGURATION (Public Client ID Only)
# ------------------------------------------------------------------------------
# Client ID is public and safe to expose in the browser

VITE_STRAVA_CLIENT_ID="your-strava-client-id"

# ⚠️  SECURITY NOTE: Client Secret Removed
# The Strava Client Secret is NO LONGER in this file for security reasons.
# It is now stored securely in Supabase Edge Function environment variables.
#
# To configure the Edge Function secret:
# 1. Go to Supabase Dashboard → Edge Functions → Configuration
# 2. Add these secrets:
#    - STRAVA_CLIENT_ID (same as above)
#    - STRAVA_CLIENT_SECRET (your secret key)
# 3. Deploy the Edge Functions:
#    - supabase/functions/strava-exchange
#    - supabase/functions/strava-refresh
#
# See EDGE_FUNCTIONS_DEPLOYMENT.md for detailed instructions

# How to get Strava API keys:
# 1. Go to https://www.strava.com/settings/api
# 2. Create a new application or use existing
# 3. Application details:
#    - Name: "City Explorer" (or your app name)
#    - Website: https://your-domain.com
#    - Authorization Callback Domain: your-domain.com
# 4. Copy the Client ID (and Client Secret for Edge Function config)
# 5. Callback URL will be: https://your-domain.com/auth/strava/callback

# ==============================================================================
# SECURITY CHECKLIST
# ==============================================================================
# ✅ .env file is in .gitignore (NEVER commit it)
# ✅ Only use anon/publishable keys in frontend (VITE_ prefix)
# ✅ Service role keys should ONLY be in Edge Functions or server
# ✅ Strava Client Secret is ONLY in Edge Function environment
# ✅ Enable RLS (Row Level Security) on all database tables
# ✅ Rotate keys immediately if accidentally exposed
# ==============================================================================
